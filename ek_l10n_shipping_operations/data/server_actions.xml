<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="1">

    <!-- Acción del servidor para crear factura de reembolso de cliente -->
    <record id="action_create_customer_reimbursement_invoice_server" model="ir.actions.server">
      <field name="name">Crear Factura de Reembolso</field>
      <field name="model_id" ref="model_ek_reimbursement_tracking"/>
      <field name="binding_model_id" ref="model_ek_reimbursement_tracking"/>
      <field name="binding_view_types">list</field>
      <field name="state">code</field>
      <field name="code">
if records:
    result = records.action_create_customer_reimbursement_invoice()
    if result:
        action = result
      </field>
      <field name="help">Crea una factura de reembolso de cliente con las líneas de seguimiento seleccionadas.</field>
    </record>

  </data>
</odoo>
