<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_purchase_order_filter" model="ir.ui.view">
        <field name="name">purchase.order.select - purchase_tier_validation</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_filter" />
        <field name="arch" type="xml">
            <filter name="order_date" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Necesita mi revisión"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['done', 'cancel','to approve'])]"
                    help="My Purchases to review"
                />
                <filter
                    name="tier_validated"
                    string="Revisiones Validadas"
                    domain="[('validated', '=', True)]"
                    help="POs validated and ready to be confirmed"
                />
            </filter>
        </field>
    </record>
    <record id="purchase_order_kpis_tree" model="ir.ui.view">
        <field name="name">purchase.order.inherit.purchase.order.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree" />
        <field name="arch" type="xml">
            <tree position="inside">
                <field
                    name="validation_status"
                    widget="badge"
                    optional="hide"
                    decoration-info="validation_status == 'pending'"
                    decoration-danger="validation_status == 'rejected'"
                    decoration-success="validation_status == 'validated'"
                />
            </tree>
        </field>
    </record>

    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form (in purchase_security)</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <field name="user_id" position="before">
                <field name="is_user_id_editable" invisible="1" />
            </field>
            <field name="user_id" position="attributes">
                <attribute name="readonly">is_user_id_editable == False</attribute>
                <attribute name="force_save">1</attribute>
            </field>
            <field name="invoice_status" position="before">
                <field name="invoice_method" groups="ek_l10n_ec_purchase.group_invoice_method_oc" />
                <field
                    name="force_invoiced"
                    groups="ek_l10n_ec_purchase.group_invoice_method_oc"
                    invisible="state not in ('purchase', 'done')"
                    readonly="state == 'done'"
                    widget="boolean_toggle"
                />
            </field>
            <xpath expr="//field[@name='partner_id']/.." position="inside">
                <field name="use_only_supplied_product" invisible="1"/>
            </xpath>
            <xpath
                expr="//field[@name='order_line']/tree/field[@name='product_id']"
                position="attributes"
            >
                <attribute name="context">
                    {
                        'restrict_supplier_id': parent.partner_id,
                        'use_only_supplied_product': parent.use_only_supplied_product,
                        'partner_id':parent.partner_id,
                        'quantity':product_qty,
                        'company_id': parent.company_id
                    }
                </attribute>
            </xpath>
            <xpath
                expr="//field[@name='order_line']/form//field[@name='product_id']"
                position="attributes"
            >
                <attribute name="context">
                    {
                        'restrict_supplier_id': parent.partner_id,
                        'use_only_supplied_product': parent.use_only_supplied_product,
                        'partner_id':parent.partner_id,
                        'quantity':product_qty,
                        'company_id': parent.company_id
                    }
                </attribute>
            </xpath>
        </field>
    </record>
</odoo>